# Copied to user_functions.env locally / written by GitHub Actions on deploy.
# Deploy workflows read both from GitHub Actions secrets (not vars):
#   secrets.ILOVELAWYER_API_BASE
#   secrets.CHAT_WONDER_API_KEY  (must match ilovelawyer-api)

ILOVELAWYER_API_BASE=http://localhost:3001
CHAT_WONDER_API_KEY=
